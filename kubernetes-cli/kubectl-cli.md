
```bash
kubectl version --client -o json # 客户端版本
kubectl cluster-info # kubernetes集群信息
kubectl config current-context # 使用哪个context
kubectl config use-context minikube # 切换context

kubectl config view

kubectl get node # 节点信息
kubectl get no # 节点信息


cat /etc/hosts # 检查不同节点的IP
sudo -i # root用户
```
